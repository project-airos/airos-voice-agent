# Airos Voice Agent - Docker Environment Configuration
# Copy this file to .env and update with your values

# ============================================================
# REQUIRED CONFIGURATION
# ============================================================

# Directory where AI models will be stored (absolute path)
# This directory will contain ~12GB of ASR and TTS models
# Example: /home/user/airos-models or /Users/yourusername/.dora/models
MODELS_DIR=/path/to/your/models

# OpenAI API Key for GPT models (required for current setup)
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# ============================================================
# OPTIONAL CONFIGURATION
# ============================================================

# Anthropic API Key (optional, for Claude models)
# Get yours at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# WebSocket server port (default: 8123)
# Change if port 8123 is already in use
PORT=8123

# Jupyter notebook settings (for development)
JUPYTER_PORT=8888
JUPYTER_TOKEN=airos

# ============================================================
# DATAFLOW CONFIGURATION
# ============================================================

# Dataflow file to use (relative to example directory)
DATAFLOW_FILE=dataflow.yml

# Name for the running dataflow instance
DATAFLOW_NAME=voice-agent

# WebSocket server node name (must match dataflow.yml)
WS_SERVER_NAME=wserver

# ============================================================
# ADVANCED OPTIONS (usually don't need to change)
# ============================================================

# Docker image tag
# IMAGE_TAG=latest

# Log level (debug, info, warn, error)
# LOG_LEVEL=info

# Enable GPU support (requires nvidia-docker)
# USE_GPU=false